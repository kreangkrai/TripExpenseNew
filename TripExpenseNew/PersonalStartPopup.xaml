<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
           x:Class="TripExpenseNew.PersonalStartPopup"
           CanBeDismissedByTappingOutsideOfPopup="True"
           Size="300,400">
    <VerticalStackLayout Padding="-1" Spacing="5">
        <Frame BackgroundColor="#1C4698" HeightRequest="50" Padding="0" WidthRequest="310">
            <Label Text="START" TextColor="White" HorizontalTextAlignment="Center" FontSize="Large" FontAttributes="Bold" HeightRequest="20" />
        </Frame>
        <Grid
             Padding="5,5"
             ColumnDefinitions="Auto,Auto"
             RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto"
             RowSpacing="10"
             VerticalOptions="Center" HorizontalOptions="Center">
            
            
            <Label Grid.Row="0" Grid.Column="0" Text="ชื่อลูกค้า/สถานที่ :" FontSize="Medium" VerticalTextAlignment="Center"></Label>
            <StackLayout Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalOptions="End">
                <CheckBox x:Name="chk_customer">
                </CheckBox>
                <Label VerticalTextAlignment="Center"  Text="ลูกค้า" FontSize="Medium">
                </Label>
            </StackLayout>
            <Entry Grid.Row="1" Grid.ColumnSpan="2" x:Name="Text_Location" Placeholder="ชื่อลูกค้า/สถานที่" />
            <Label Grid.Row="2" Grid.Column="0" Text="Job :" FontSize="Medium" />
            <Entry Grid.Row="3" Grid.ColumnSpan="2" x:Name="Text_Job" Placeholder="Job" />
            <Label Grid.Row="4" Grid.Column="0" Text="เลขไมล์เริ่มต้น :" FontSize="Medium" />
            <Entry Grid.Row="5" Grid.ColumnSpan="2" x:Name="Text_Mileage" Placeholder="เลขไมล์เริ่มต้น" Keyboard="Numeric" />
            <HorizontalStackLayout Grid.Row="6" Grid.Column="1" Spacing="10" HorizontalOptions="Center">
                <Button Text="ตกลง" Clicked="OnSubmitClicked" BackgroundColor="Green"/>
                <Button Text="ยกเลิก" Clicked="OnCancelClicked" BackgroundColor="Red" />
            </HorizontalStackLayout>
        </Grid>
    </VerticalStackLayout>
</toolkit:Popup>
